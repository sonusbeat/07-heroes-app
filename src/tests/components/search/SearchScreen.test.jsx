import SearchScreen from '../../../components/search/SearchScreen';

describe('Pruebas en el componente <SearchScreen />', () => {
  test('Deberia de pasar esto', () => {
    expect(true).toBe(true);
  })

});
